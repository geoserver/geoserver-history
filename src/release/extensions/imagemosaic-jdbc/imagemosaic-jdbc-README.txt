In order to install the Imagemosaic-jdbc  extension you have to:

copy the jar contained in this package into geoserver/WEB-INF/lib

Depending on the used database, the proper JDBC Driver has to be installed

The documentation is in the geotools user guide.

A tutorial is in the geoserver user guide.

