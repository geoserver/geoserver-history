In order to install the feature-pregeneralized  extension you have to:

copy the jar files contained in this package into geoserver/WEB-INF/lib

The documentation is in the geotools user guide.

A tutorial is in the geoserver user guide.

