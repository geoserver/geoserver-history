<?xml version="1.0" encoding="ISO-8859-1"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.geoserver</groupId>
    <artifactId>community</artifactId>
    <version>1.7.3-SNAPSHOT</version>
  </parent>

  <groupId>org.geoserver</groupId>
  <artifactId>wmslayout</artifactId>
  <packaging>jar</packaging>
  <name>Web Map Service Module With Custom Layouting</name>

  <dependencies>
    <dependency>
      <groupId>org.geoserver</groupId>
      <artifactId>main</artifactId>
      <version>1.7.3-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.geoserver</groupId>
      <artifactId>wms</artifactId>
      <version>1.7.3-SNAPSHOT</version>
    </dependency>
  </dependencies>

  <!-- =========================================================== -->
  <!--     Build Configuration                                     -->
  <!-- =========================================================== -->
  <build>
    <plugins>
      <!-- ====    Code Formatting  ============================== -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>jalopy-maven-plugin</artifactId>
        <configuration>
          <srcExcludesPattern>${jalopy.srcExcludesPattern}</srcExcludesPattern>
          <testExcludesPattern>${jalopy.srcExcludesPattern}</testExcludesPattern>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>format</goal>
            </goals>
            <phase>${jalopy.phase}</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <excludes>
            <!-- this test seems to make other unrelated tests to fail and I have no idea why -->
            <!--exclude>**/KMLNetworkLinkTransformerTest.java</exclude>
            <exclude>**/KMLLegendTransformerTest.java</exclude>
            <exclude>**/KMLVectorTransformerTest.java</exclude-->
          </excludes>
          <includes>
            <include>**/*Test.java</include>
          </includes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>cobertura-maven-plugin</artifactId>
        <configuration>
          <instrumentation>
            <!-- ignores>
             <ignore>**/*Exception.class</ignore>
            </ignores-->
            <excludes>
              <exclude>**/*Exception.class</exclude>
            </excludes>
            <!-- includes>
              <include>**/*Test.class</include>
            </includes-->
          </instrumentation>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
