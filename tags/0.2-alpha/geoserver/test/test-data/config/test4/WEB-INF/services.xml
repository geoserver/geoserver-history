<?xml version="1.0" encoding="UTF-8"?>
<!--
Service level configuration
-->
<serverConfiguration>
<global>
<!--
Defines the logging level.  Common options are SEVERE,
WARNING, INFO, CONFIG, FINER, FINEST, in order of
Increasing statements logged.
-->
<loggingLevel>FINER</loggingLevel>
<!--
The base URL where this servlet will run.  If running locally
then http://localhost:8080 (or whatever port you're running on)
should work.  If you are serving to the world then this must be
the location where the geoserver servlets appear
-->
<URL>http://office.refractions.net:8081/geoserver/</URL>
<!--
Sets the max number of Features returned by GetFeature
-->
<maxFeatures value = "20000" />
<!--
Whether newlines and indents should be returned in 
XML responses.  Default is false
-->
<verbose value = "true" />
<!--
Sets the max number of decimal places past the zero returned in
a GetFeature response.  Default is 4
-->
<numDecimals value = "8" />
<!--
Sets the global character set.  This could use some more testing
from international users, but what it does is sets the encoding
globally for all postgis database connections (the charset tag
in FeatureType), as well as specifying the encoding in the return
xml header and mime type.  The default is UTF-8.  Also be warned
that GeoServer does not check if the CharSet is valid before
attempting to use it, so it will fail miserably if a bad charset
is used.
-->
<charSet value = "ISO-8859-1" />
<!--
Define a base url for the location of the wfs schemas.
By default GeoServer loads and references its own at
<URL>/data/capabilities. Uncomment to enable.  The
standalone Tomcat server needs SchemaBaseUrl defined
for validation.
-->
<SchemaBaseUrl>C:\Java\workspace\Geoserver-Model\tests\test2/data/capabilities/</SchemaBaseUrl>
</global>
<services>
<service type = "WFS" enabled = "true" >
<!--
Service elements, needed for the capabilities document
Title and OnlineResource are the two required
-->
<name>FreeFS</name>
<title>GeoServer TEST Web FeatureServer</title>
<abstract>This is a test server used for cite tests</abstract>
<keywords>
<keyword>WFS</keyword>
<keyword>TEST</keyword>
<keyword>Cite</keyword>
<keyword>GeoServer</keyword>
</keywords>
<onlineResource>http://office.refractions.net:8081/</onlineResource>
<fees>NONE</fees>
<accessConstraints>NONE</accessConstraints>
<maintainer>Jody Garnett</maintainer>
</service>
<service type = "WMS" enabled = "true" >
<!--
Service elements, needed for the capabilities document
Title and OnlineResource are the two required
-->
<name>FreeWMS</name>
<title>GeoServer Cite Test Web Map Server</title>
<abstract>This is a test server used for cite tests</abstract>
<keywords>
<keyword>WMS</keyword>
<keyword>TEST</keyword>
<keyword>Cite</keyword>
<keyword>GeoServer</keyword>
</keywords>
<onlineResource>http://office.refractions.net:8081/</onlineResource>
<fees>NONE</fees>
<accessConstraints>NONE</accessConstraints>
<maintainer>Jody Garnett</maintainer>
</service>
</services>
</serverConfiguration>
