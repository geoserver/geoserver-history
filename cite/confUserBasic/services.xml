<?xml version="1.0" encoding="iso-8859-1"?>
<!--
Service level configuration
-->
<serverConfiguration>
  <global>

      <!-- Defines the logging level.  Common options are SEVERE,
           WARNING, INFO, CONFIG, FINE, FINER, FINEST, in order of
           Increasing statements logged.
       -->
     <loggingLevel>WARNING</loggingLevel>


     <!-- Sets the maximum features returned by GeoServer.  This used to
          be necessary, due to out of memory errors.  The server is much
          more scalable now, but admins may use it to limit the amount of
          data returned, so naive clients don't ask for megabytes.
       -->
     <!--     
     <maxFeatures value="10000"/>
     -->

     <!-- Sets if the xml returned should have line feeds and indents. 
          The default is true.  Setting it to false will cut down on
          the amount of blank spaces returned to clients, and is recommended
          if humans will not read the output -->
     <verbose value="true"/>

     <!-- Sets the max number of decimal places past the zero returned in
          the gml coordinates of a GetFeature response.  Default is 4 -->
     <numDecimals value="8"/>

     <!-- Example Contact information -->
     <ContactInformation>
       <ContactPersonPrimary>
         <ContactPerson>You</ContactPerson>
       </ContactPersonPrimary>
       <ContactPosition>Geoserver Administrator</ContactPosition>
     </ContactInformation>
  </global>
  <services>
    <service type="WFS" enabled="true">
      <!-- Service elements, needed for the capabilities document -->
      <!-- Title and OnlineResource are the two required -->
      <name>My GeoServer WFS</name>
      <title>My GeoServer WFS</title>
      <abstract>
The GeoServer project is a full transactional Java (J2EE) 
implementation of the OpenGIS Consortium's Web Feature Server 
specification, with an integrated WMS. It is free software, 
available under the GPL 2.0 license.

Users who would like to access and modify their geographic data over the
Internet using flexible, industry-approved standards should take a look
at GeoServer or one of the existing commercial Web Feature Servers.
     </abstract>
      <keywords>
        <keyword>WFS</keyword>
        <keyword>WMS</keyword>
        <keyword>GEOSERVER</keyword>
      </keywords>
      <onlineResource>http://geoserver.sourceforge.net/html/index.php</onlineResource>
      <fees>NONE</fees>
      <accessConstraints>NONE</accessConstraints>
      <maintainer>http://jira.codehaus.org/secure/BrowseProject.jspa?id=10311</maintainer>
      <gmlPrefixing value="true"/>
    </service>

    <service type="WMS" enabled="true">
      <!-- Service elements, needed for the capabilities document -->
      <!-- Title and OnlineResource are the two required -->
      <name>My GeoServer WMS</name>
      <title>My GeoServer WMS</title>
      <abstract>
The GeoServer project is also an implementation of the OpenGIS
Consortium's Web Map Server specification.

If you are interested in a deeper examination of the importance 
of the GeoServer Project, you should read our FAQ, where we have 
more descriptive explanations.
     </abstract>
      <keywords>
        <keyword>WFS</keyword>
        <keyword>WMS</keyword>
        <keyword>GEOSERVER</keyword>
      </keywords>
      <onlineResource>http://geoserver.sourceforge.net/html/index.php</onlineResource>
      <fees>NONE</fees>
      <accessConstraints>NONE</accessConstraints>
      <maintainer>http://jira.codehaus.org/secure/BrowseProject.jspa?id=10311</maintainer>
    </service>

     <!-- This tag is to run the zserver module. -->
     <!-- Set the run attribute to false or comment the tag -->
     <!-- If you don't want the module to run. -->
     <!-- The Port tag specifies which port to run on. -->
     <service type="Z39.50" enabled="false">
      <port>3210</port>
     </service>
  </services>
</serverConfiguration>
