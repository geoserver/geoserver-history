GEOSERVER 1.3.0 DATA STORE EXTRAS README

This package contains data store implementations which are still in an experimental state. Please report any bugs with jira (http://jira.codehaus.org/browse/GEOS). 

Any other issues can be discussed on the mailing list (http://lists.sourceforge.net/lists/listinfo/geoserver-users).

Currently contains the following data stores:

- $$$

INSTALLATION

1. Unpackage the archive into the root of your geoserver installation (ie. The directory containing the WEB-INF directory). Note that if running from a Jetty install the server root is located under the 'server/geoserver' directory.

2. Restart GeoServer.

